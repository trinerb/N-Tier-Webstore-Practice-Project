@model Category

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Create Category</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post">
            @*we don't need this line if our id is called Id(because default). If called something else, we need to specify. Else we will not update existing record, but create a new one.(because id is 0) *@
            <input asp-for="Id" hidden />
            <div class="border p-3 mt-4">

                @*<div asp-validation-summary="ModelOnly"></div>  //*@
                <div class="mb-3 row p-1">
                    @*We use tag-helpers. When using label asp-for="name", if name has a value, it will automatically be displayed*@
                    <label asp-for="Name" class="p-0"></label>
                    <input asp-for="Name" disabled class="form-control" /> @* field will be disabled for delete*@
                    @* <span asp-validation-for="Name" class="text-danger"></span>  dont need validation for delete*@
                </div>
                <div class="mb-3 row p-1">
                    <label asp-for="DisplayOrder" class="p-0"></label>
                    <input asp-for="DisplayOrder" disabled class="form-control" />
                    <span asp-validation-for="DisplayOrder" class="text-danger"></span>
                </div>

                <div class="row">
                    <div class="col-6 col-md-3">
                        <button type="submit" class="btn btn-danger form-control">Delete</button>
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-controller="Category" asp-action="Index" class="btn btn-outline-secondary form-control">Back To List</a>
                    </div>
                </div>

            </div>
        </form>
    </div>
</div>




